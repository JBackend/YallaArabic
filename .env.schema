# Environment Schema - defines expected variables and validation rules
# This file is safe to commit (contains no secrets)
# Managed by Varlock - run `varlock load` to validate

# Linear API Key for project management integration
# @type=string(startsWith=lin_api_) @required @sensitive
LINEAR_API_KEY=

# Node environment
# @type=enum(development,production,test)
NODE_ENV=

# ElevenLabs API Key for audio generation
# @type=string(startsWith=sk_) @required @sensitive
ELEVENLABS_API_KEY=

# ElevenLabs Voice ID (optional, defaults to multilingual voice)
# @type=string @optional
ELEVENLABS_VOICE_ID=

# Google Analytics 4 Measurement ID (client-side, not sensitive)
# @type=string(startsWith=G-) @optional
NEXT_PUBLIC_GA_ID=
