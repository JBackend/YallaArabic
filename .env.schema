# Environment Schema - defines expected variables and validation rules
# This file is safe to commit (contains no secrets)

# Linear API Key for project management integration
# @type=string(startsWith=lin_api_) @required @sensitive
LINEAR_API_KEY=

# Node environment
# @type=enum(development,production,test)
NODE_ENV=
